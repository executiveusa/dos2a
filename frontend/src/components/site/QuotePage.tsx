"use client";
import { MessageCircle } from "lucide-react";import { useLanguage } from "@/lib/language";import { siteContent } from "@/lib/site-content";import PublicShell from "./PublicShell";import QuoteForm from "./QuoteForm";
export default function QuotePage(){const{lang}=useLanguage();const c=siteContent.quote;return <PublicShell><section className="page-hero page-hero--compact"><p className="eyebrow">{lang==="es"?"Cotizar":"Get a quote"}</p><h1>{c.title[lang]}</h1><p>{c.body[lang]}</p></section><section id="la-genio" className="quote-layout"><aside className="quote-helper"><MessageCircle size={26}/><p className="eyebrow">La Genio</p><h2>{lang==="es"?"Habla como hablarías con una persona.":"Talk the way you would talk to a person."}</h2><p>{lang==="es"?"Por ahora, usa el campo final para contar la idea con tus propias palabras. Esta información prepara el mismo brief que usará La Genio cuando la conversación guiada esté activa.":"For now, use the final field to describe the idea in your own words. This prepares the same brief La Genio will use when guided conversation is active."}</p><p className="small-note">{lang==="es"?"No inventaremos una conversación de IA antes de que el sistema gobernado esté conectado y probado.":"We will not fake an AI conversation before the governed system is connected and tested."}</p></aside><QuoteForm/></section></PublicShell>}
