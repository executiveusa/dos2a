"use client";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { useLanguage } from "@/lib/language";
import { siteContent } from "@/lib/site-content";
import PublicShell from "./PublicShell";
export default function ServicesPage(){const {lang}=useLanguage();const c=siteContent.services;return <PublicShell><section className="page-hero"><p className="eyebrow">{lang==="es"?"Servicios":"Services"}</p><h1>{c.title[lang]}</h1><p>{c.intro[lang]}</p></section><section className="section"><div className="service-grid service-grid--detail">{c.items.map((item,i)=>{const href=["/servicios/audio","/servicios/video","/servicios/iluminacion","/servicios/escenarios"][i];const card=<article className={href?"service-card service-card--link":"service-card"} key={item.title.es}><span>{String(i+1).padStart(2,"0")}</span><h2>{item.title[lang]}</h2><p>{item.body[lang]}</p>{href&&<span className="service-card-more">{lang==="es"?"Ver servicio":"View service"}<ArrowRight size={14}/></span>}</article>;return href?<Link href={href} key={item.title.es} className="service-card-link">{card}</Link>:card})}</div></section><section className="closing-cta"><h2>{lang==="es"?"No tienes que saber qué equipo pedir.":"You do not need to know which equipment to request."}</h2><p>{lang==="es"?"Cuéntanos el objetivo, el lugar, la fecha y el público. A partir de ahí podemos definir el alcance.":"Tell us the objective, place, date, and audience. From there we can define the scope."}</p><Link className="button button--light" href="/cotizar">{lang==="es"?"Cuéntanos tu evento":"Tell us about your event"}<ArrowRight size={17}/></Link></section></PublicShell>}
