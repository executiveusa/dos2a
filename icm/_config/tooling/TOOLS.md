# Required tooling

Before feature coding verify/install globally:
- JCodeMunch MCP: token-efficient symbol-first repo context.
- RTK: compact shell/tool output; verify correct package with `rtk gain`.
- Caveman: Lite mode for internal/status output only.
- Scroll World skill: used selectively for cinematic stage.
- codebase-to-course: Phase 19 owner handoff only.

Stage-specific skills are loaded minimally. Do not globally preload the entire skills library into every worker.
