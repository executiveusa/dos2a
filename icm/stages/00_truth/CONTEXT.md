# Stage 00 — Truth & baseline

One job: establish current repository, production, claims, constraints, architecture truth, open risks, and evidence before editing product behavior.

Inputs: current master, deployed public site, governance, prior verified decisions.
Outputs: `output/truth-baseline.md`, `output/verified-claims.json`, `output/risk-register.json`, updated STATUS.

Do not implement features. Verify what is actually present and live. Record conflicts between README, code, deployments, and prior plans. Use broad audit only because this is an explicit audit gate; still prefer targeted retrieval.
