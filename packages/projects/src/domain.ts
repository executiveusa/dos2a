import type { ProposalVersion } from "../../proposals/src/domain";
export interface ConfirmedProject{tenantId:string;leadId:string;sourceProposalId:string;sourceProposalVersionId:string;confirmedTotalMinor:number;currency:string;proposalSnapshot:Record<string,unknown>;eventDate?:string;venue?:string}
export function projectFromAcceptedProposal(args:{tenantId:string;leadId:string;proposal:ProposalVersion;proposalStatus:"accepted";eventDate?:string;venue?:string}):ConfirmedProject{if(args.proposalStatus!=="accepted")throw new Error("proposal_not_accepted");return{tenantId:args.tenantId,leadId:args.leadId,sourceProposalId:args.proposal.proposalId,sourceProposalVersionId:args.proposal.versionId,confirmedTotalMinor:args.proposal.totalMinor,currency:args.proposal.currency,proposalSnapshot:structuredClone(args.proposal.snapshot),eventDate:args.eventDate,venue:args.venue}}
