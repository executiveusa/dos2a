# Design — ICM autonomous harness

ICM owns context boundaries; GRINIONS owns execution gates. GitHub remains canonical release truth.

Workers load one stage at a time. JCodeMunch is first code-context tool. RTK compresses verbose shell output. Caveman Lite is internal-only. Skills are loaded minimally per stage. The harness does not mount production credentials or bypass branch protection.
