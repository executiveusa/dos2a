# Stage 01 proposal — repository reconciliation

## Objective
Make `master` and the README tell the same repository truth before product implementation begins.

## Scope
- verify open PR state;
- distinguish production, canonical foundation, and parked legacy surfaces;
- replace stale README architecture/completion claims;
- preserve legacy code rather than deleting without dependency evidence.

No runtime behavior, database schema, auth, or production UI change.
